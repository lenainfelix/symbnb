 <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
     <a class="navbar-brand" href="{{path('homepage')}}">SymBnB</a>
     <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02"
         aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
         <span class="navbar-toggler-icon"></span>
     </button>

     <div class="collapse navbar-collapse" id="navbarColor02">
         <ul class="navbar-nav mr-auto">
             <li class="nav-item mr-2">
                 <a class="nav-link" href="#"><i class="far fa-chart-bar"></i>Dashboard</a>
             </li>
             <li class="nav-item mr-2">
                 <a class="nav-link" href="{{path('admin_ads_index')}}"><i class="far fa-file-alt"></i>Annonces</a>
             </li>
             <li class="nav-item mr-2">
                 <a class="nav-link" href="{{path('admin_bookings_index')}}"><i class="fas fa-concierge-bell"></i>Réservations</a>
             </li>
             <li class="nav-item mr-2">
                 <a class="nav-link" href="{{path('admin_users_index')}}"><i class="fas fa-users"></i>Utilisateurs</a>
             </li>
             <li class="nav-item mr-2">
                 <a class="nav-link" href="{{path('admin_comments_index')}}"><i class="far fa-comment"></i>Commentaires</a>
             </li>
         </ul>
         <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('homepage') }}" class="nav-link" target="_blank">
                    <i class="fas fa-eye"></i>
                    Voir le site
                </a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown toggle" href="#" data-toggle='dropdown' id='accountDropdownLink'>
                        <img src="{{app.user.picture}}" alt="{{app.user.fullName}}" class="avatar avatar-mini"> {{app.user.fullName}}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink"> 
                            <a class="dropdown-item " href="{{path('admin_account_logout')}}">Deconnexion</a>
                    </div>
                </li>
        
            {% endif %}

         </ul>
     </div>
 </nav>
 <div class="container "style="height: 4rem;">></div>

 {% for label, messages in app.flashes %}
 <div class="container mt-3">
     <div class="alert alert-{{label}}">
         {% for message in messages %}
         <p>{{message | raw}}</p>
         {% endfor %}
     </div>
 </div>
 {% endfor %}