{% extends "base.html.twig" %}

{% block title %}Edition de votre profil{% endblock %}

{% block body %}
   <div class="container">
        <h1>Modifier le profil</h1>

        {{form_start(form)}}
           
        {{form_widget(form)}}
    
        <button type="submit" class="btn btn-success">Enregistrer les modifications</button>

        {{form_end(form)}}
    </div>
{% endblock %}

