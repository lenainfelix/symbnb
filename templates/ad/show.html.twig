{% extends "base.html.twig" %}

{% block title %}
    {{ad.title}}
{% endblock %}

{% block body %}
    <div class="myClass" style="background-image:url({{ ad.coverImage }})">
        <div class="container d-flex flex-column bg">
            <h1>{{ad.title}}</h1>
            <p>{{ad.introduction}}</p>
            <p class="h2">
             <strong>{{ ad.rooms }} chambres</strong> pour <strong>{{ad.price}}&euro;</strong> par nuit          
            </p>
            <a href="#" class="btn btn-primary ml-3 mb-4 w-25">Réserver !</a>
        </div>
    </div>
    <div class="container mt-3">
        <div class="row">
            <div class="col-md-8">
                {{ad.content|raw}}

                <div id="carouselExampleIndicators" class="carousel slide mb-5" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {% for image in ad.images %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{loop.index0}}" class="{% if loop.first %} active {% endif %}"></li>
                        {% endfor %}
                    </ol>
                    <div class="carousel-inner">
                        
                        {% for image in ad.images %}
                            <div class="carousel-item {% if loop.first %} active {% endif %}">
                                <img src="{{image.url}}" class="d-block w-100" alt="First slide">
                                 <div class="carousel-caption d-none d-md-block text-center">
                                     <h5>{{ad.title}}</h5>
                                     <p>{{image.caption}}</p>
                                 </div>
                            </div>
                        {% endfor %}
                        
                       
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>
            </div>
            
            <div class="col">
                <div class="row">
                    <div class="col-4">
                        <img src="http://placehold.it/64x64" alt="Avatar de Joseph">
                    </div>
                    <div class="col">
                        <h3>Joseph Dupont</h3>
                        <span class="badge badge-primary">3 annonces</span>
                    </div>
                </div>
                <p class='mt-2 d-flex justify-content'>Lorem ipsum dolor sit amet consectetur, adipisicing elit. Dolores quasi natus dolorem ducimus quas earum sequi magnam reiciendis suscipit eum!</p>
                <p>Quod, accusamus eius doloribus laudantium iure delectus eligendi eos similique perferendis et ab odio, voluptate, aspernatur vero cumque nihil aliquid!</p>
                <p>Sint veniam quas, voluptate iste cum molestiae dignissimos numquam aliquid fuga vitae recusandae laboriosam eius porro laborum voluptas. Nostrum, consectetur!</p>
            </div>
        </div>
    </div>
    
{% endblock %}