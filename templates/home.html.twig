{% extends 'base.html.twig' %}

{% block title %}
    Bienvenue dans le meilleur réseau d'apartements.
{% endblock  %}

{% block body %}
<div class="container mt-3">
    <div class="jumbotron">
        <h1 class="display-3">Bienvenue sur SymBnB!</h1>
        <p class="lead">Nous mettons en relations les voyageurs et les propriétaires qui veulent d'opportunités et gagner de l'argent en proposant leur bien à la location!</p>
        <hr class="my-4">
        <p>Vous êtes voyageur ? Et vous voulez voir où vous pourrez vous loger lors de votre prochain déplacement ?</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="#" role="button">Voir nos annonces.</a>
        </p>
    </div>
    <hr>
    <h2 class=" mt-3 mb-4">Nos appartments stars</h2>
    <div class="row ">
        
        {% for data in bestAds %}
           {% include "ad/_ad.html.twig" with {'ad': data.ads} %} 
        {% endfor %}
    
    </div>
    <hr>
    <h2 class=' mt-3 mb-4'>Nos propriétaires stars.</h2>
    <div class="row">
        {% for data in bestUsers %}
            <div class="col">
                <div class="card border-success mb-3">
                    <div class="card-body">
                        <div class="card-header d-flex align-items-center">
                            <img src="{{data.users.picture}}" class='avatar mr-3' alt="Avatar de {{data.users.fullName}}" class="float-left">
                            <div>
                                <h4 class="card-title">{{data.users.fullName}}</h4>
                                {% include "shared/rating.html.twig" with {'rating' : data.avgRatings }%}
                            </div>
                        </div>
                    
                        <div class="card-text">
                            <p>{{data.users.introduction}}</p>
                            <a href="{{ path('user_show',{'slug': data.users.slug}) }}" class='btn btn-success float-right'>En savoir plus!</a>
                        </div>
                    </div>
                    
                </div>
            </div>
        {% endfor %} 
    </div>

</div>
{% endblock  %}
